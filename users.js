import server from './index';

export default class {
  static getById(email = null, password = null) {
    
  }
  
  static getUser(params) {
    
  }
  
  static getUsers(page = 0, perPage = 10) {
    
  }
}